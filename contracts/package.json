{
  "name": "zk-proof-registry",
  "version": "1.0.0",
  "description": "ZK Proof Registry Smart Contract",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run contracts/deploy.js --network polygon-zkevm-testnet",
    "deploy:local": "hardhat run contracts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "chai": "^4.3.10",
    "ethereum-waffle": "^4.0.10",
    "ethers": "^5.7.2",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0"
  }
}